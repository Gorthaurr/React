import React, {useEffect, useContext, useState} from 'react';
import {CardResponse} from '../models/response/CardResponse';
import Card from './Card'

const GetUsers: React.FC = () => {
    const [users, setUsers] = useState<CardResponse[]>([])


    return(
        <div>

        </div>
    )
}